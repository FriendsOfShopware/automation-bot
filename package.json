{
  "name": "automation-bot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "build-actions": "cd actions/fetch-token && bunx --bun esbuild --minify --platform=node --format=cjs --bundle src/main/main.ts --outdir=dist/main && bunx --bun esbuild --minify --platform=node --format=cjs --bundle src/post/main.ts --outdir=dist/post && cd ../report-data && bunx --bun esbuild --minify --platform=node --format=cjs --bundle src/main/main.ts --outdir=dist/main"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.5.2",
    "wrangler": "^4.56.0",
    "@actions/core": "^2.0.2"
  },
  "dependencies": {
    "@cfworker/jwt": "^6.0.1",
    "@octokit/auth-app": "^7.1.3",
    "@octokit/core": "^6.1.2",
    "@octokit/webhooks": "^13.3.0",
    "hono": "^4.11.7"
  }
}
